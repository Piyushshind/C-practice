#include <stdio.h>

int main(){
    int row ,col ;
    scanf("Enter the value of row :- %d " , & row);
    scanf("Enter the value of colums :- %d " , & col);
    int arr[row][col];

    return    0;
}